import React from 'react'

import Link from 'next/link'

const Form = ({ type,
post,
setPost,
submitting,
handleSubmit }) => {
    return (
        <section className="w-full max-w-full">Submit</section>
    )
}

export default Form
